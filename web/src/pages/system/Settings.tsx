import { Typography } from 'antd'

const { Title } = Typography

function Settings() {
  return (
    <div>
      <Title level={3}>系统设置</Title>
      <p>系统管理 - 设置页面</p>
    </div>
  )
}

export default Settings
