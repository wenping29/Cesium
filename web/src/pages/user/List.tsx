import { Typography } from 'antd'

const { Title } = Typography

function UserList() {
  return (
    <div>
      <Title level={3}>用户列表</Title>
      <p>用户管理 - 列表页面</p>
    </div>
  )
}

export default UserList
